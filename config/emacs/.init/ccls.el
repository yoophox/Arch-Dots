(use-package ccls
  :ensure t
  :after lsp-mode)
  ;;:hook (c-mode c++-mode objc-mode)
  ;;:config
  ;;(setq lsp-keymap-prefix "C-l")
  ;;(setq ccls-executable "ccls") ; Path to ccls binary if not in PATH
  ;;(setq lsp-ccls-sem-highlight-method 'lsp-semantic-tokens))
